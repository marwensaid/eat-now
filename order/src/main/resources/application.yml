server:
  port: 8082
spring:
  application:
    name: order
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info,prometheus
#  health:
#    probes:
#      enabled: true

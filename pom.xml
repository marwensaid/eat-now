<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Coordonnées Maven du projet parent -->
    <groupId>com.eatnow</groupId>
    <artifactId>springboot-eat-now</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging> <!-- Important: 'pom' indique que c'est un projet parent/agrégateur -->

    <name>Springboot Eat Now - Parent</name>
    <description>Parent project for the Eat Now microservices application</description>

    <!-- Héritage du parent Spring Boot pour une gestion simplifiée des dépendances -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.1.5</version>
        <relativePath/>
    </parent>

    <!-- Propriétés centralisées pour les versions -->
    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2022.0.4</spring-cloud.version>
        <springdoc-openapi.version>2.2.0</springdoc-openapi.version>
    </properties>

    <!-- Déclaration des sous-modules -->
    <modules>
        <module>menu-service</module>
        <module>order-service</module>
        <module>delivery-service</module>
    </modules>

    <!-- Gestion centralisée des versions de dépendances pour les modules enfants -->
    <dependencyManagement>
        <dependencies>
            <!-- BOM (Bill of Materials) pour Spring Cloud -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Documentation API (Swagger/OpenAPI) -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc-openapi.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Plugin Spring Boot pour la création des JARs exécutables -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>

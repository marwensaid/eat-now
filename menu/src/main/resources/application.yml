server:
  port: 8081
spring:
  application:
    name: menu
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info,prometheus
#  health:
#    probes:
#      enabled: true

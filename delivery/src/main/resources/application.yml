server:
  port: 8083
spring:
  application:
    name: delivery
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info,prometheus
#  health:
#    probes:
#      enabled: true

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eat-now-chart.fullname" . }}-test-connection"
  labels:
    {{- include "eat-now-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eat-now-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

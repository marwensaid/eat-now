apiVersion: v1
kind: Service
metadata:
  name: {{ include "eat-now-chart.fullname" . }}
  labels:
    {{- include "eat-now-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eat-now-chart.selectorLabels" . | nindent 4 }}

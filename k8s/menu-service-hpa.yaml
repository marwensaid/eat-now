apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: menu-service-hpa
  labels:
    app: menu-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: menu-service
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
